---
description: Recompile the LaTeX CV to PDF
---

To update the PDF after changing the `.tex` file, run the following command in the `Smart-CV` directory:

```bash
bash compile.sh
```

// turbo
```bash
bash compile.sh
```
